# Dependencies for this repo's own scripts.
#
# prepare_dataset.py is stdlib-only. verify_dataset.py's real-loader check
# imports the FL-YuE2 data module (which needs numpy) and reads audio through
# soundfile. Lower bounds, not pins: Colab's preinstalled versions are fine.
numpy>=2.0
soundfile>=0.12.1
